<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= listing.wastetype %> - Details</title>

    <link rel="stylesheet" href="/css/show.css">
</head>
<body>

<header class="navbar">
    <a href="/" class="back-btn">⬅ Back</a>
    <h2 class="title">Listing Details</h2>
</header>

<div class="container">

    <div class="card">

        <img src="<%= listing.image %>" alt="<%= listing.wastetype %>" class="listing-img" >

        <h2><%= listing.wastetype %></h2>

        <p><strong>Quantity:</strong> <%= listing.quantity %> kg</p>
        <p><strong>Location:</strong> <%= listing.location %></p>

        <div class="button-group">

            <a href="/farmer/<%= listing._id %>/edit" class="edit-btn">Edit</a><br><br>
            <!-- DELETE BUTTON -->
            <form action="/farmer/<%= listing._id %>?_method=DELETE" method="POST">
                <button type="submit" class="delete-btn">Delete</button>
            </form>
            

             

        </div>

    </div>

</div>

</body>
</html>
